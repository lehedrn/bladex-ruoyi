# BladeX Saber3 业务逻辑公式手册

> 数据来源：四份报告的规则清单合并去重
> 规则编号规范：AR-xxx（底层架构）、BE-xxx（后端）、FE-xxx（前端）、CT-xxx（接口契约）

---

## 第1章：规则总览

### 1.1 规则汇总矩阵

| 分类 | AR 规则 | BE 规则 | FE 规则 | 合计 |
|------|---------|---------|---------|------|
| 计算规则 | AR-001 ~ AR-008 | BE-005 | 无 | 9 |
| 流程规则 | AR-009 ~ AR-017 | BE-007/009/010/015/026/031/034/037/041/046/047/048 | FE-004/013/018/020/021/024/032/036/038/044 | 38 |
| 约束规则 | AR-018 ~ AR-023 | BE-001/002/003/004/006/008/012/017/018/021/022/023/025/027/030/032/033/035/036/038/042/043/044/045/050 | FE-001/002/003/005/006/007/009/010/011/014/023/028/029/033/034/035/039/040/041/042/043/047/048/049 | 55 |
| 权限规则 | AR-024 ~ AR-031 | BE-011/013/014/016/019/020/024/039/049/050 | FE-008/017/019/022/025/037 | 22 |
| 异常规则 | AR-032 ~ AR-037 | BE-028/029/040 | FE-012/015/016/030/050 | 15 |
| **总计** | **37** | **50** | **50** | **139** |

> 注：BE-050 在底层架构和后端报告中都有涉及，已去重。

---

## 第2章：计算规则

| 编号 | 规则描述 | 显式/隐式 | 来源 | 生效范围 |
|------|---------|----------|------|---------|
| AR-001 | JWT Token 包含租户ID、用户ID、角色列表，使用配置的密钥签名 | 显式 | 底层架构报告 | 全局 |
| AR-002 | 分页参数默认限制最大页数（pageLimit），防止全量查询 | 显式 | 底层架构报告 | 全局 SQL |
| AR-003 | Redis 序列化支持 JSON 和 ProtoStuff 两种策略，默认为 JSON | 显式 | 底层架构报告 | Redis 操作 |
| AR-004 | OSS 文件名基于租户模式拼接：`tenant-id/filename` | 显式 | 底层架构报告 | 文件上传 |
| AR-005 | 数据源名称提取：去除 beanName 中的 `dataSource` 后缀 | 隐式 | 底层架构报告 | Druid 监控 |
| AR-006 | API 文档自动注入 Authorization、Blade-Auth、Tenant-Id 三个请求头 | 显式 | 底层架构报告 | API 文档 |
| AR-007 | 敏感词过滤使用 DFA 算法构建 Trie 树 | 隐式 | 底层架构报告 | 文本输入 |
| AR-008 | Swagger Tag 排序通过解析 Tag Description 中的数字 | 隐式 | 底层架构报告 | API 文档 |
| BE-005 | 许可证模式下账号额度从 DES 解密后的 LicenseKey 中读取 | 显式 | 后端报告 | 租户级 |

---

## 第3章：流程规则

| 编号 | 规则描述 | 显式/隐式 | 来源 | 生效范围 |
|------|---------|----------|------|---------|
| AR-009 | 请求先注入 TraceID，再通过 BladeContext 持有租户/用户上下文 | 显式 | 底层架构报告 | 全局请求链 |
| AR-010 | OAuth2 Token 授予通过 TokenGranterFactory 选择对应 Granter | 显式 | 底层架构报告 | OAuth2 端点 |
| AR-011 | 文件上传路径拼接：`savePath/upload/` 注册为静态资源 | 显式 | 底层架构报告 | Web 资源 |
| AR-012 | 多拦截器链执行顺序：Basic → Sign → Token → Client | 隐式 | 底层架构报告 | 全局请求链 |
| AR-013 | MyBatis 拦截器链：租户过滤 → 数据审计 → 分页 | 显式 | 底层架构报告 | SQL 执行 |
| AR-014 | 动态数据源处理器链：Header → Session → TenantId → SpelExpression | 显式 | 底层架构报告 | 数据源路由 |
| AR-015 | 数据审计在 MyBatis 数据变更拦截器之前注册 | 显式 | 底层架构报告 | SQL 执行 |
| AR-016 | API 日志通过 Spring Event 异步发布 | 显式 | 底层架构报告 | 全局请求链 |
| AR-017 | XSS 过滤通过 Jackson 反序列化器和 Form 拦截器双重处理 | 显式 | 底层架构报告 | 请求处理 |
| BE-007 | 用户删除联动清理 5 张关联表（user_dept/user_oauth/user_web/user_app/user_other） | 显式 | 后端报告 | 全局 |
| BE-009 | 审核通过前置条件：已分配角色/部门/岗位 | 显式 | 后端报告 | 全局 |
| BE-010 | 新注册用户默认为未激活状态（IN_ACTIVE） | 显式 | 后端报告 | 全局 |
| BE-015 | 授予角色权限时递归清理子角色的无权菜单 | 显式 | 后端报告 | 全局 |
| BE-026 | 部门创建时自动维护 ancestors 字段 | 显式 | 后端报告 | 全局 |
| BE-031 | 用户解锁操作清除 Redis 中的锁定计数 | 显式 | 后端报告 | 租户内 |
| BE-034 | 主管状态可切换（0↔1） | 显式 | 后端报告 | 全局 |
| BE-037 | 发送短信验证码前校验手机号已注册 | 显式 | 后端报告 | 租户内 |
| BE-041 | 流程上传文件仅支持 .bpmn20.xml 后缀 | 显式 | 后端报告 | 全局 |
| BE-046 | 租户回收站三阶段删除：recycle → pass → remove | 显式 | 后端报告 | 全局 |
| BE-047 | Excel 导入支持覆盖模式（isCovered=1 时更新已有用户） | 显式 | 后端报告 | 全局 |
| BE-048 | 用户按平台分表存储（WEB/APP/OTHER），通过 userType 区分 | 显式 | 后端报告 | 全局 |
| FE-004 | 登录 grant_type 根据 captchaMode 动态切换（captcha/password） | 显式 | 前端报告 | 认证模块 |
| FE-013 | Token 刷新时并发请求队列合并（isRefreshing 锁） | 显式 | 前端报告 | 全局请求 |
| FE-018 | 已登录访问 /login → 自动跳转 / | 显式 | 前端报告 | 路由守卫 |
| FE-020 | 导航至非登录页且 isTab !== false → 自动添加标签页 | 显式 | 前端报告 | 路由守卫 |
| FE-021 | meta.target 为 true → window.open 打开外部链接 | 显式 | 前端报告 | 路由守卫 |
| FE-024 | 菜单数据从 localStorage 恢复，冷启动时立即注册动态路由 | 隐式 | 前端报告 | 应用启动 |
| FE-032 | CRUD 删除操作默认弹出确认对话框 | 显式 | 前端报告 | CRUD 混入 |
| FE-036 | 用户列表支持审核模式（auditMode）切换 | 显式 | 前端报告 | 用户管理 |
| FE-038 | 导出用户数据通过 Blob 流下载为 Excel | 显式 | 前端报告 | 用户管理 |
| FE-044 | 登录页面根据 isMacOs getter 切换不同 UI 主题 | 隐式 | 前端报告 | 登录模块 |

---

## 第4章：约束规则

| 编号 | 规则描述 | 显式/隐式 | 来源 | 生效范围 |
|------|---------|----------|------|---------|
| AR-018 | 租户模式下默认返回 ADMIN_TENANT_ID 当用户无租户信息 | 显式 | 底层架构报告 | SQL 租户过滤 |
| AR-019 | 批量更新限制通过 batchUpdateLimit 参数控制 | 显式 | 底层架构报告 | 数据变更 |
| AR-020 | MyBatis 默认日志关闭（NoLoggingImpl），除非显式配置 | 显式 | 底层架构报告 | SQL 执行 |
| AR-021 | Redis 限流使用 Lua 脚本保证原子性 | 显式 | 底层架构报告 | Redis 限流 |
| AR-022 | Nonce 防重放攻击，本地使用 ConcurrentMap 存储 | 隐式 | 底层架构报告 | 签名验证 |
| AR-023 | Swagger 默认排除 /error 和 /actuator/** 路径 | 显式 | 底层架构报告 | API 文档 |
| BE-001 | 用户密码使用 DigestUtil 加密（BCrypt）存储 | 显式 | 后端报告 | 全局 |
| BE-002 | 同一租户下用户账号唯一 | 显式 | 后端报告 | 租户内 |
| BE-003 | 同一租户下用户手机号唯一 | 显式 | 后端报告 | 租户内 |
| BE-004 | 租户账号数量受限于产品包额度 | 显式 | 后端报告 | 租户级 |
| BE-006 | 用户不可删除自身账号 | 显式 | 后端报告 | 全局 |
| BE-008 | 用户修改自身信息时强制忽略角色/部门/岗位/账号/主管字段 | 显式 | 后端报告 | 全局 |
| BE-012 | 主管列表仅查询 isLeader=1 且 status=正常 的用户 | 显式 | 后端报告 | 租户内 |
| BE-017 | 角色不可删除有子节点的记录 | 显式 | 后端报告 | 全局 |
| BE-018 | 角色别名列表中排除 ADMINISTRATOR | 隐式 | 后端报告 | 全局 |
| BE-021 | 菜单 code/name/path 全局唯一 | 显式 | 后端报告 | 全局 |
| BE-022 | 菜单父节点必须为菜单类型（category=1） | 显式 | 后端报告 | 全局 |
| BE-023 | 菜单不可删除有子节点的记录 | 显式 | 后端报告 | 全局 |
| BE-025 | 部门不可删除有子节点的记录 | 显式 | 后端报告 | 全局 |
| BE-027 | 部门不可选择自身作为父节点 | 显式 | 后端报告 | 全局 |
| BE-030 | 用户登录失败次数超限将被锁定（Redis 计数） | 显式 | 后端报告 | 租户内 |
| BE-032 | 重置密码使用 Param 表配置的默认密码 | 显式 | 后端报告 | 全局 |
| BE-033 | 修改密码需确认新密码一致 | 显式 | 后端报告 | 全局 |
| BE-035 | 部门主管查询仅返回 status=正常 且 isDeleted=未删除 的用户 | 显式 | 后端报告 | 全局 |
| BE-036 | 验证码从 Redis 读取并匹配（不区分大小写） | 显式 | 后端报告 | 全局 |
| BE-038 | 手机号使用 SM2 加密传输 | 显式 | 后端报告 | 全局 |
| BE-042 | 物理表列表过滤 ACT_/FLW_ 开头的 Flowable 内部表 | 隐式 | 后端报告 | 全局 |
| BE-043 | 数据源 URL 中的 &amp; 需替换为 & | 隐式 | 后端报告 | 全局 |
| BE-044 | 用户-部门支持多对多关系（通过 blade_user_dept 中间表） | 显式 | 后端报告 | 全局 |
| BE-045 | 数据权限和接口权限通过 scopeCategory 字段区分 | 显式 | 后端报告 | 全局 |
| BE-050 | 验证码发送前校验租户和手机号存在性，防止恶意发送 | 显式 | 后端报告 | 租户内 |
| FE-001 | 用户名密码登录时，密码使用 SM2 公钥加密后传输 | 显式 | 前端报告 | 登录模块 |
| FE-002 | 手机验证码登录时，手机号使用 SM2 公钥加密 | 显式 | 前端报告 | 登录模块 |
| FE-003 | 注册时密码使用 SM2 公钥加密 | 显式 | 前端报告 | 注册模块 |
| FE-005 | Token 通过 Cookie 存储，Key 为 `saber3-access-token` | 显式 | 前端报告 | 全局认证 |
| FE-006 | RefreshToken 通过 Cookie 存储，Key 为 `saber3-refresh-token` | 显式 | 前端报告 | 全局认证 |
| FE-007 | 请求必须携带 `Blade-Requested-With: BladeHttpRequest` 头 | 显式 | 前端报告 | 全局请求 |
| FE-009 | Token 请求头名称为 `Blade-Auth`（可配置） | 显式 | 前端报告 | 全局请求 |
| FE-010 | Token 支持 bearer 明文和 crypto 加密两种传输方式 | 显式 | 前端报告 | 全局请求 |
| FE-011 | 请求/响应数据可开启 AES-CBC 报文加密 | 显式 | 前端报告 | 全局请求 |
| FE-014 | HTTP 超时时间 10 秒 | 显式 | 前端报告 | 全局请求 |
| FE-023 | 动态路由组件 name 设为 to.fullPath（keep-alive 缓存标识） | 隐式 | 前端报告 | 路由守卫 |
| FE-028 | 权限数据持久化到 sessionStorage（非 localStorage） | 隐式 | 前端报告 | 权限模块 |
| FE-029 | 锁屏状态和锁屏密码持久化到 sessionStorage | 显式 | 前端报告 | 锁屏模块 |
| FE-033 | CRUD 列表默认每页 10 条 | 隐式 | 前端报告 | 列表页 |
| FE-034 | 用户新增/更新时，deptId/roleId/postId/leaderId 多值使用逗号拼接 | 显式 | 前端报告 | 用户管理 |
| FE-035 | 用户更新时，脱敏字段（phone, email）仅提交已修改的值 | 显式 | 前端报告 | 用户管理 |
| FE-039 | 身份证号码校验：18位格式、地区编码、校验位算法 | 显式 | 前端报告 | 全局校验 |
| FE-040 | 手机号码校验：正则 `^1[0-9]{10}$` | 显式 | 前端报告 | 全局校验 |
| FE-041 | 邮箱校验：正则匹配标准邮箱格式 | 显式 | 前端报告 | 全局校验 |
| FE-042 | 姓名校验：2-4位中文字符 | 显式 | 前端报告 | 全局校验 |
| FE-043 | validatenull 判断：boolean/number 永不为空 | 隐式 | 前端报告 | 全局工具 |
| FE-045 | iframe 路由 URL 中 `&` 替换为 `#` 避免路由参数冲突 | 显式 | 前端报告 | iframe 模块 |
| FE-046 | iframe URL 支持 `${token}` 等变量替换 | 显式 | 前端报告 | iframe 模块 |
| FE-047 | 页面标题格式：`页面名 | Saber`（通过 i18n） | 显式 | 前端报告 | 全局 |
| FE-048 | keep-alive 缓存列表基于 tagsKeep getter | 显式 | 前端报告 | 全局 |
| FE-049 | 水印文本根据 website.watermark.mode 开关控制 | 显式 | 前端报告 | 全局布局 |

---

## 第5章：权限规则

| 编号 | 规则描述 | 显式/隐式 | 来源 | 生效范围 |
|------|---------|----------|------|---------|
| AR-024 | Token 拦截器校验请求头中的 Authorization，失败返回 401 | 显式 | 底层架构报告 | 全局请求链 |
| AR-025 | 签名拦截器校验请求签名（timestamp + sign），防止请求篡改 | 显式 | 底层架构报告 | 全局请求链 |
| AR-026 | @PreAuth 注解通过 SpEL 表达式校验用户角色/权限 | 显式 | 底层架构报告 | Controller 方法 |
| AR-027 | @IsAdmin 注解校验用户是否为管理员角色 | 显式 | 底层架构报告 | Controller 方法 |
| AR-028 | 数据权限通过 @DataAuth + DataScopeInterceptor 在 SQL 层追加 WHERE | 显式 | 底层架构报告 | SQL 执行 |
| AR-029 | Basic 认证拦截器校验 HTTP Basic Auth 头 | 显式 | 底层架构报告 | 全局请求链 |
| AR-030 | Client 认证拦截器校验客户端 ID 和密钥 | 显式 | 底层架构报告 | 全局请求链 |
| AR-031 | API Key 校验通过 BladeApiKeyHandler 接口可扩展 | 显式 | 底层架构报告 | 全局请求链 |
| BE-011 | 第三方注册用户分配 guest 角色（roleId = -1） | 显式 | 后端报告 | 全局 |
| BE-013 | 非超管不可配置超管角色权限 | 显式 | 后端报告 | 全局 |
| BE-014 | 非超管/管理员不可配置管理员角色权限 | 显式 | 后端报告 | 全局 |
| BE-016 | 非超管不可创建超管角色 | 显式 | 后端报告 | 全局 |
| BE-019 | 超级管理员可访问全部菜单 | 显式 | 后端报告 | 全局 |
| BE-020 | 普通用户菜单通过租户产品包 menuId 过滤 | 显式 | 后端报告 | 租户级 |
| BE-024 | 非超管角色只可查看本级及以下部门数据 | 显式 | 后端报告 | 租户级 |
| BE-039 | 租户产品包限制用户可访问的菜单范围 | 显式 | 后端报告 | 租户级 |
| BE-049 | authRoutes 返回的路径-权限映射用于前端动态权限控制 | 显式 | 后端报告 | 全局 |
| FE-008 | 请求必须携带 OAuth2 Basic 认证头（除非 authorization=false） | 显式 | 前端报告 | 全局请求 |
| FE-017 | 未登录访问非公开页面 → 跳转 /login | 显式 | 前端报告 | 全局路由 |
| FE-019 | 锁屏状态下访问非锁屏页 → 强制跳转 /lock | 显式 | 前端报告 | 路由守卫 |
| FE-022 | 路由 meta.isAuth === false → 无需 Token 即可访问 | 显式 | 前端报告 | 路由守卫 |
| FE-025 | 按钮权限递归提取叶子节点的 code 构建权限对象 | 显式 | 前端报告 | 权限模块 |
| FE-037 | 审核操作仅限 admin 角色执行 | 显式 | 前端报告 | 用户管理 |

---

## 第6章：异常规则

| 编号 | 规则描述 | 显式/隐式 | 来源 | 生效范围 |
|------|---------|----------|------|---------|
| AR-032 | 全局异常通过 BladeErrorMvcAutoConfiguration 统一处理，返回标准 R 格式 | 显式 | 底层架构报告 | 全局 |
| AR-033 | 错误日志通过 ErrorLogListener 异步发送到日志服务 | 显式 | 底层架构报告 | 全局 |
| AR-034 | OAuth2 异常统一通过 OAuth2ExceptionHandler 处理 | 显式 | 底层架构报告 | OAuth2 端点 |
| AR-035 | Excel 导入通过 DataListener 分批处理，防止 OOM | 隐式 | 底层架构报告 | Excel 导入 |
| AR-036 | HTTP 客户端支持自动重试（RetryInterceptor + RetryPolicy） | 显式 | 底层架构报告 | HTTP 请求 |
| AR-037 | 分布式事务异常触发 Seata 全局回滚 | 显式 | 底层架构报告 | 全局事务 |
| BE-028 | 租户创建/修改后需清除 SYS_CACHE 缓存 | 显式 | 后端报告 | 全局 |
| BE-029 | 修改/删除部门/字典/参数后需清除对应缓存 | 显式 | 后端报告 | 全局 |
| BE-040 | 路由权限信息缓存（MENU_CACHE, key=auth:routes:roleId） | 显式 | 后端报告 | 全局 |
| FE-012 | 401 响应自动触发 Token 刷新，刷新失败跳转登录 | 显式 | 前端报告 | 全局请求 |
| FE-015 | HTTP 状态码 200-500 视为有效响应（不抛网络错误） | 隐式 | 前端报告 | 全局请求 |
| FE-016 | OAuth2 错误码（>2000）优先显示 error_description | 显式 | 前端报告 | 全局请求 |
| FE-030 | 错误日志 localStorage 持久化，支持批量上报 | 显式 | 前端报告 | 日志模块 |
| FE-050 | 开发环境检测 DevTools 打开时弹窗警告并 5 秒后关闭页面 | 显式 | 前端报告 | 开发环境 |

---

## 第7章：规则交叉验证（前后端一致性）

### 7.1 约束规则对齐

| 业务规则 | 前端实现 | 后端实现 | 一致性 |
|---------|---------|---------|--------|
| 密码加密传输 | FE-001/002/003：SM2 公钥加密 | BE-001/038：后端 BCrypt 存储 / SM2 解密 | ✅ 一致 |
| 账号唯一性 | 前端表单校验 | BE-002：selectCount 校验 | ✅ 一致 |
| 手机号唯一性 | 前端表单校验 | BE-003：selectCount 校验 | ✅ 一致 |
| 手机号格式 | FE-040：正则 `^1[0-9]{10}$` | BE-037：发送前校验已注册 | ✅ 一致 |
| 邮箱格式 | FE-041：正则校验 | 后端 User 实体无显式校验 | ⚠️ 仅前端校验 |
| 身份证格式 | FE-039：18位完整算法校验 | 后端无显式身份证校验 | ⚠️ 仅前端校验 |
| 姓名格式 | FE-042：2-4位中文字符 | 后端无显式姓名校验 | ⚠️ 仅前端校验 |
| 分页默认值 | FE-033：默认每页 10 条 | MyBatis-Plus 默认 10 | ✅ 一致 |

### 7.2 权限规则对齐

| 业务规则 | 前端实现 | 后端实现 | 一致性 |
|---------|---------|---------|--------|
| 登录认证 | FE-017：未登录跳转 /login | AR-024：Token 拦截器 | ✅ 一致 |
| 按钮权限 | FE-025：v-if + permission 对象 | BE-049：authRoutes 返回映射 | ✅ 一致 |
| 菜单权限 | FE-022：meta.isAuth | AR-026/027：@PreAuth/@IsAdmin | ✅ 一致 |
| 审核操作 | FE-037：admin 角色判断 | 后端 admin 角色校验 | ✅ 一致 |
| 锁屏保护 | FE-019：锁屏强制跳转 | 无后端对应 | ⚠️ 仅前端 |

### 7.3 计算规则对齐

| 业务规则 | 前端实现 | 后端实现 | 一致性 |
|---------|---------|---------|--------|
| Token 生成 | 无前端计算 | AR-001：JWT 签名 | ✅ 后端独控 |
| OSS 文件名 | 无前端计算 | AR-004：租户模式拼接 | ✅ 后端独控 |

### 7.4 异常规则对齐

| 业务规则 | 前端实现 | 后端实现 | 一致性 |
|---------|---------|---------|--------|
| Token 过期 | FE-012：401 自动刷新 | AR-024：返回 401 | ✅ 一致 |
| 全局异常处理 | FE-016：OAuth2 错误码优先 | AR-034：OAuth2ExceptionHandler | ✅ 一致 |
| 错误日志 | FE-030：前端收集上报 | AR-033：ErrorLogListener | ✅ 一致 |

### 7.5 差异汇总

| 差异类型 | 数量 | 说明 |
|---------|------|------|
| ✅ 完全一致 | 15 条 | 前后端对同一规则的实现一致 |
| ⚠️ 仅前端校验 | 3 条 | 邮箱、身份证、姓名仅前端有格式校验 |
| ⚠️ 仅前端保护 | 1 条 | 锁屏保护无后端对应 |
| ❌ 不一致 | 0 条 | 无前后端矛盾的情况 |

---

**手册生成日期**: 2026-05-21
