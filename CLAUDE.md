# CLAUDE.md

## 上下文加载

读取本文件后，必须立即读取 `docs/bladex-ruoyi/info.md`，获取项目技术栈、目录结构、常用命令等信息。

## 项目概述

bladex-ruoyi 是一个对 BladeX 和 RuoYi 两个主流开发框架进行系统性、完整性分析的仓库。当前处于规划阶段，两个框架的源码放在 `sources/` 目录下作为参考，后续目标是实现用户体系互通、数据互通、框架融合。

## AI 行为偏好

### 回复风格
- 回复使用中文，技术术语、代码标识符保持英文原文
- 框架分析时详尽深入，覆盖实现原理、架构对比、设计取舍
- 主动展示分析细节和发现，不需要用户追问就展开完整说明
- 输出形式优先使用：对比表格 > 架构图/流程图 > 代码示例 > 文档总结

### 代码风格
- 代码注释使用中文
- 不过度设计，不提前引入不需要的模块或依赖（YAGNI）
- 优先编辑现有文件，不轻易创建新文件
- `sources/` 目录是参考学习的只读资料，不可修改

### 协作偏好
- **讨论优先**：方案先讨论、列待办、存依据，未经确认不执行。逐项推进，每次聚焦一个待办
- **不擅自行动**：不擅自删除/引入/优化
- **主动推荐**：可主动建议，经同意后形成待办文件推进
- **上下文连贯**：基于已有文档和结论工作，不脱离基础重来

## 文档规范

**存放目录：** `docs/bladex-ruoyi/`

**更新策略：** 执行操作后主动思考是否需更新文档，如需要则按写入规范执行

**写入规范：**
- 单次写入超过 200 行时，分段完成（每次 100-200 行）
- 文档内容过大时，先按主题或模块拆分为分文档，依次写入
- 拆分后的文档通过索引文档组织，避免上下文丢失

**命名规范：**
- 文件名、目录名使用小写英文字母，单词间用连字符 `-` 连接

## 环境与工具

### drawio SVG 导出（WSL2 GPU 限制）

drawio snap 在无 GPU 的 WSL2 环境中频繁报 `eglInitialize failed`，部分 `.drawio` 文件无法通过 CLI 导出 SVG。

**解决流程**：
1. 先尝试 `drawio --no-sandbox -x -f svg -e -b 10 -o out.svg in.drawio`
2. 失败则使用项目内置 fallback 脚本：`scripts/drawio-to-svg.py in.drawio out.svg`
3. 导出的 SVG 文件名统一为 `xxx.svg`（不要 `xxx.drawio.svg`），与 markdown 文档中的引用路径 `./diagrams/xxx/xxx.svg` 保持一致
4. fallback 脚本能解析 drawio 原生 style（fillColor、strokeColor、fontSize、rounded 等），生成带分组背景、正交连线、原生配色的 SVG

### Agent 并行执行
- `Agent` 工具的 `description` 参数是必填项，遗漏会导致启动失败
- 所有 Agent 统一使用 `model: sonnet`
