# CLAUDE.md

## 项目概述

本项目是 BladeX 与 RuoYi 的对比分析仓库，目的是理解两套框架的架构差异与实现方式，为后续**融合、打通数据服务**奠定基础。

- 当前处于**分析研究阶段**，尚未进入正式开发
- 两个项目地位平等，分析时需覆盖双方

## 回复风格

- 使用中文回复，技术术语保留原文（如 Spring Boot、OAuth2、Starter）
- 框架分析时**详尽深入**，覆盖实现原理、架构对比、设计取舍
- 输出形式优先使用：对比表格 > 架构图/流程图 > 代码示例 > 文档总结
- 分析产出物写入 `docs/` 目录下的独立 `.md` 文件

## 代码风格

- 代码注释使用中文
- 修改代码时遵循**最小改动原则**：只改被要求修改的部分，不做无关重构
- 保持与原项目一致的缩进和命名风格

## 协作偏好

- **先计划后执行**：每次改动前先说明计划和涉及的文件
- 涉及修改文件时，**先列出文件列表**，确认后再修改
- 框架分析类任务，优先输出结论文档到 `docs/` 目录

## 文档规范

**更新策略：** 执行操作后主动思考是否需更新文档，如需要则按写入规范执行

**写入规范：**
- 单次写入超过 200 行时，分段完成（每次 100-200 行）
- 文档内容过大时，先按主题或模块拆分为分文档，依次写入
- 拆分后的文档通过索引文档组织，避免上下文丢失

**命名规范：**
- 文件名、目录名使用小写字母，单词间用连字符 `-` 连接
- 不要使用空格，中文文件名允许（如 `01-概述.md`）

**其他约定：**
- 所有分析文档放在 `docs/` 目录下，按主题分子目录
- 分析文档命名使用小写英文 + 连字符（如 `blade-vs-ruoyi-auth.md`）
- 每个分析文档开头包含：分析日期、涉及的源文件路径、核心结论摘要
- CLAUDE.md 和项目信息文件变更时，需在 commit message 中说明原因

## 上下文加载

读取本文件后，必须立即读取 `docs/bladex-ruoyi/info.md`，获取项目技术栈、目录结构、常用命令等信息。
